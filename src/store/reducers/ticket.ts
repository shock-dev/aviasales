const initialState = {
    tickets: [],
    isLoading: true
}

const tickets = (state = initialState, action: any) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default tickets;
