import React from 'react';
import logoImg from '../assets/logo.png';

const Logo = () => {
  return (
    <div className="logo">
      <img src={logoImg} alt="Logo" />
    </div>
  );
};

export default Logo;
